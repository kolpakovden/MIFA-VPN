# Telegram-боты

Проект использует **два независимых бота**:

1. **Бот уведомлений** — оповещает о новых подключениях
2. **Бот управления** — позволяет добавлять/удалять пользователей через Telegram

---

## Бот уведомлений (`check_users.sh`)

### Назначение
Каждую минуту проверяет лог Xray и при появлении нового IP отправляет уведомление в Telegram с геолокацией.

### Установка

1. Получи токен у [@BotFather](https://t.me/botfather) и Chat ID у [@userinfobot](https://t.me/userinfobot)
2. Отредактируй [`scripts/check_users.sh`](../scripts/check_users.sh):
   ```bash
   BOT_TOKEN="твой_токен"
   CHAT_ID="твой_chat_id"
   ```
3. Сделай скрипт исполняемым:
   ```bash
   chmod +x scripts/check_users.sh
   ```
4. Добавь в cron:
   ```bash
   crontab -e
   * * * * * /полный/путь/к/scripts/check_users.sh
   ```

### Пример уведомления
```
🔔 Новое подключение к VPN!
📍 IP: 80.83.237.47
🏙️ Город: Irkutsk
🌍 Регион: Irkutsk Oblast
🌎 Страна: Russia
📡 Провайдер: Mobile TeleSystems
🕒 Время: 24.02.2026 19:53:01
```

---

## Бот управления (`bot.py`)

### Назначение
Позволяет администратору управлять пользователями Xray через Telegram.

### Команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `/add Имя` | Добавить нового пользователя | `/add Наташа` |
| `/list` | Список всех пользователей | `/list` |
| `/del email` | Удалить пользователя | `/del natalya@myserver.com` |
| `/key email [порт]` | Получить ключ | `/key natalya@myserver.com 8443` |
| `/restart` | Перезапустить Xray | `/restart` |
| `/help` | Показать помощь | `/help` |

### Установка

```bash
# Перейти в папку проекта
cd /opt/pet_vless_telegram

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install python-telegram-bot python-dotenv

# Настроить переменные
cp .env.example .env
nano .env  # вставить токен и chat_id
```

### Запуск как сервис

```bash
sudo cp scripts/xray-tg-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now xray-tg-bot
sudo systemctl status xray-tg-bot
```

### Логи бота
```bash
sudo journalctl -u xray-tg-bot -f
```
